import { createServer as _createServer } from './cloudvm/createServer';
import { deleteServer as _deleteServer } from './cloudvm/deleteServer';

export const createServer = _createServer;
export const deleteServer = _deleteServer;
